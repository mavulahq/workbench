{
  "name": "@mavula/workbench",
  "version": "0.1.0",
  "private": true,
  "description": "MAVULA Workbench - durable worker runtime and public platform status API.",
  "author": "EstandarMustaq <estandarmustaq@mavula.io>",
  "license": "AGPL-3.0-only",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "guardian:check": "node scripts/guardian.mjs",
    "start": "node dist/main.js",
    "start:dev": "ts-node-dev --respawn --transpile-only src/main.ts",
    "test": "jest --runInBand",
    "test:e2e": "jest --config jest.e2e.config.js --runInBand",
    "test:all": "jest --runInBand && jest --config jest.e2e.config.js --runInBand"
  },
  "dependencies": {
    "@mavula/settlements": "workspace:*",
    "@nestjs/common": "^10.0.0",
    "@nestjs/core": "^10.0.0",
    "@nestjs/platform-express": "^10.0.0",
    "bullmq": "^5.0.0",
    "ioredis": "^5.3.2",
    "reflect-metadata": "^0.1.13",
    "rxjs": "^7.8.0"
  },
  "devDependencies": {
    "@nestjs/testing": "^10.0.0",
    "@types/jest": "^29.0.0",
    "@types/supertest": "^6.0.2",
    "jest": "^29.0.0",
    "supertest": "^6.3.3",
    "ts-jest": "^29.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": "22.22.3",
    "pnpm": "10.33.0"
  }
}
